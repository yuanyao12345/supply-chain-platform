# 花湖国际机场供应链金融平台 - 傻瓜式操作指南

> 🎯 本指南专为零基础用户设计，按照步骤操作即可完成部署和修改。

---

## 📋 第一部分：首次部署（只需做一次）

### 步骤1：注册PythonAnywhere账号

1. 打开浏览器，访问：https://www.pythonanywhere.com/
2. 点击右上角的 "Sign up" 按钮
3. 填写注册信息：
   - Username（用户名）：建议使用 yuanyao
   - Email（邮箱）：您的邮箱地址
   - Password（密码）：设置密码
4. 点击 "Create account" 完成注册
5. 登录邮箱，点击验证链接激活账号

### 步骤2：创建Web应用

1. 登录PythonAnywhere
2. 点击顶部菜单的 **"Web"**
3. 点击 **"Add a new web app"**
4. 选择 **"Manual configuration"**
5. 选择 **"Python 3.9"**
6. 点击 **"Next"** 完成创建

### 步骤3：运行自动部署脚本

1. 点击顶部菜单的 **"Consoles"**
2. 点击 **"Bash"** 打开Bash控制台
3. 复制以下命令，粘贴到控制台，按回车执行：

```bash
cd ~
rm -rf supplychain
git clone https://github.com/yuanyao12345/supply-chain-platform.git supplychain
cd supplychain
pip install -r requirements.txt
python -c 'from app import app, db; app.app_context().push(); db.create_all(); print("Database created!")'
python -c 'from app import app, db, Admin; app.app_context().push(); admin = Admin.query.filter_by(username="admin").first(); if not admin: admin = Admin(username="admin", password="admin123"); db.session.add(admin); db.session.commit(); print("Admin account created!"); else: print("Admin account already exists, skipping...")'
echo "代码部署完成！"
```

4. 等待命令执行完成（看到"代码部署完成！"提示）

### 步骤4：配置Web应用

1. 点击顶部菜单的 **"Web"**
2. 找到您的应用：`yuanyao.pythonanywhere.com`
3. 找到 **"Code"** 部分：
   - **Source code**：改为 `/home/yuanyao/supplychain`
   - **Working directory**：改为 `/home/yuanyao/supplychain`
4. 找到 **"WSGI configuration file"** 链接，点击进入
5. 删除所有内容，粘贴以下代码：

```python
import sys
import os

# 添加项目路径
path = '/home/yuanyao/supplychain'
if path not in sys.path:
    sys.path.append(path)

# 设置环境变量
os.environ.setdefault('FLASK_ENV', 'production')
os.environ.setdefault('SECRET_KEY', 'your-secret-key-change-this')

# 导入应用
from app import app as application
```

6. 点击 **"Save"** 保存
7. 点击 **"Reload"** 按钮重启应用
8. 等待几秒钟，点击链接访问您的网站

### 步骤5：验证部署

1. 访问：https://yuanyao.pythonanywhere.com
2. 看到首页说明部署成功！
3. 测试以下功能：
   - 企业注册
   - 银行注册
   - 融资申请
   - 后台管理（用户名：admin，密码：admin123）
   - 融资审批

---

## 🔄 第二部分：修改网站（可多次操作）

### 场景1：修改文字内容

**示例：修改首页标题**

1. **在本地修改**
   - 打开文件：`templates/index.html`
   - 找到 `<title>` 标签
   - 修改文字内容
   - 保存文件

2. **提交到GitHub**
   ```bash
   git add templates/index.html
   git commit -m "修改首页标题"
   git push origin main
   ```

3. **在PythonAnywhere更新**
   ```bash
   cd ~/supplychain
   git pull origin main
   ```
   然后点击 **"Reload"** 按钮

### 场景2：修改颜色

**示例：修改导航栏颜色**

1. **在本地修改**
   - 打开文件：`templates/index.html`
   - 找到 `.header` 样式
   - 修改颜色代码
   - 保存文件

2. **提交到GitHub**
   ```bash
   git add templates/index.html
   git commit -m "修改导航栏颜色"
   git push origin main
   ```

3. **在PythonAnywhere更新**
   ```bash
   cd ~/supplychain
   git pull origin main
   ```
   然后点击 **"Reload"** 按钮

### 场景3：添加新页面

**示例：添加"帮助中心"页面**

1. **创建页面文件**
   - 复制 `templates/about.html`
   - 重命名为 `templates/help.html`
   - 修改页面内容

2. **添加路由**
   - 打开 `app.py`
   - 在 `# 关于我们` 下方添加：
   ```python
   # 帮助中心
   @app.route('/help')
   def help():
       return render_template('help.html')
   ```

3. **添加导航链接**
   - 打开 `templates/index.html`
   - 在导航栏添加：
   ```html
   <a href="{{ url_for('help') }}">帮助中心</a>
   ```

4. **提交到GitHub**
   ```bash
   git add templates/help.html templates/index.html app.py
   git commit -m "添加帮助中心页面"
   git push origin main
   ```

5. **在PythonAnywhere更新**
   ```bash
   cd ~/supplychain
   git pull origin main
   ```
   然后点击 **"Reload"** 按钮

---

## 🚀 快速更新命令（复制粘贴即可）

### 本地提交命令
```bash
git add .
git commit -m "描述你的修改"
git push origin main
```

### PythonAnywhere更新命令
```bash
cd ~/supplychain
git pull origin main
```
然后点击 **"Reload"** 按钮

---

## 📁 文件结构说明

```
项目根目录/
├── app.py                 # 主程序（路由、数据库模型）
├── requirements.txt       # Python依赖包
├── templates/            # HTML模板文件夹
│   ├── index.html       # 首页
│   ├── about.html       # 关于我们页面
│   ├── register_company.html  # 企业注册
│   ├── register_bank.html     # 银行注册
│   ├── loan_application.html  # 融资申请
│   ├── admin_login.html       # 后台登录
│   ├── admin_dashboard.html   # 后台管理
│   ├── bank_login.html        # 银行登录
│   └── bank_dashboard.html    # 银行审批
└── instance/
    └── supply_chain.db  # 数据库文件
```

---

## 🎨 常用修改示例

### 修改1：更改网站标题
**文件**：`templates/index.html`
**位置**：`<title>` 标签
```html
<title>新标题 - 花湖国际机场供应链金融平台</title>
```

### 修改2：更改导航栏颜色
**文件**：`templates/index.html`
**位置**：`.header` 样式
```css
.header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### 修改3：修改联系电话
**文件**：`templates/index.html`
**位置**：页脚部分
```html
<p>联系电话：027-87654321</p>
```

### 修改4：添加新导航链接
**文件**：`templates/index.html`
**位置**：`<nav class="nav">` 内部
```html
<a href="{{ url_for('about') }}">关于我们</a>
```

---

## ❌ 常见错误及解决方法

### 错误1：git push 失败
**症状**：提示 "failed to push"
**解决**：
```bash
git pull origin main
git push origin main
```

### 错误2：PythonAnywhere上 git pull 失败
**症状**：提示 "error: Your local changes..."
**解决**：
```bash
cd ~/supplychain
git stash
git pull origin main
git stash pop
```

### 错误3：网站显示500错误
**症状**：页面显示 "Internal Server Error"
**解决**：
1. 检查错误日志：在PythonAnywhere的Web页面点击 "Error log"
2. 修复问题后点击 "Reload"

### 错误4：修改不生效
**症状**：修改后网站没有变化
**解决**：
1. 确认已执行 `git push origin main`
2. 在PythonAnywhere执行 `git pull origin main`
3. 点击 "Reload" 按钮
4. 清除浏览器缓存（Ctrl+F5）

---

## 📝 修改流程总结

```
本地修改 → 保存文件 → git add . → git commit → git push → 
PythonAnywhere: git pull → Reload
```

**记忆口诀**：
- 本地：add → commit → push
- 线上：pull → reload

---

## 🎯 检查清单

每次修改后，请检查：
- [ ] 本地文件已保存
- [ ] 已执行 `git add .`
- [ ] 已执行 `git commit -m "描述"`
- [ ] 已执行 `git push origin main`
- [ ] PythonAnywhere已执行 `git pull origin main`
- [ ] 已点击 "Reload" 按钮
- [ ] 网站显示正常

---

## 📞 需要帮助？

如果遇到问题：
1. 查看错误日志（PythonAnywhere Web页面的 "Error log"）
2. 检查每一步是否按顺序执行
3. 复制错误信息寻求帮助

---

## ✅ 恭喜！

您已经掌握了部署和修改网站的完整流程！

**现在您可以：**
- ✅ 部署网站到PythonAnywhere
- ✅ 修改网站内容和样式
- ✅ 添加新页面和功能
- ✅ 更新线上网站

**下一步建议：**
- 尝试修改一些简单的文字
- 尝试修改颜色
- 尝试添加一个新页面
- 熟悉整个流程

祝您使用愉快！
